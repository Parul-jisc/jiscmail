/**
 * Contains global variables.
 */

// Viewport size below which mobile styling is applied.
const MEDIUM_SMALL_AND_BELOW_VIEWPORT_WIDTH_PX = 757;

// Delay (in milliseconds) to remove overlay class from 
// the faceted search refine menu.
const FACETED_SEARCH_MENU_OVERLAY_REMOVE_DELAY_MS = 500;
